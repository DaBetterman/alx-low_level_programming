/**
 *_abs- a function that computes the absolute value of an integer.
 *@i: The number that is being computed.
 *Return: Always 0 (Success)
 */
#include "main.h"
int _abs(int i)
{
if (i < 0)
return (-i);
else
{
return (i);
}
}
