#include <stdio.h>

/**
 * main - Printing the alphabet using putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char i;
char a[] = "abcdefghijklmnopqrstuvwxyz";
for (i = 'a'; i <= 'z'; i++)
{
putchar(i);
}
return (0);
}
