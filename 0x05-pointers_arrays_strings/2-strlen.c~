/**
 *_strlen - A function that returns the length of a string.
 *@s: The pointer variable that will be pointing to another pointer.
 *
 *Return: Return the value of s.
 */

#include "main.h"
#include <string.h>
int _strlen(char *s)
{
h = strlen(s);
return (h);
}
