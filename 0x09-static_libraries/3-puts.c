/**
 *_puts - A function that prints a string, followed by a new line.
 *@str: Will be returned to main.
 *
 *Return: Always 0 (Success)
 */
#include "main.h"
#include <stdio.h>

void _puts(char *str)
{
puts(str);
}
