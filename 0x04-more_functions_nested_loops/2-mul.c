/**
 *mul - a function that multiplies two integers.
 *@a: to be multiplied with b
 *@b: to be multiplied with a
 *Return: result of two numbers that was multiplied.
 */

#include "main.h"
int mul(int a, int b)
{
int result = a * b;
return (result);
}
